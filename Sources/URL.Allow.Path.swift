import Foundation

extension URL.Allow {
    enum Path: String {
        case
        pagead,
        recaptcha,
        swg,
        account,
        ads,
        tr,
        plugins = "v2.2",
        subscription_offer = "subscription-offer"
    }
}
