import Foundation

extension URL.Allow {
    enum Subdomain: String {
        case
        platform,
        interactive,
        tr,
        accounts,
        tarifvergleich,
        mobileads,
        sourcepointcmp,
        tracking,
        cookies
    }
}
