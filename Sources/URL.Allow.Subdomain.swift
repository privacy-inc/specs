import Foundation

extension URL.Allow {
    enum Subdomain: String {
        case
        platform,
        interactive,
        tr,
        accounts,
        tarifvergleich,
        mobileads,
        sourcepoint,
        sourcepointcmp,
        tracking,
        cookies
    }
}
